{
  // The pod's default assistant — what `POD_DEFAULT` resolves to, and what the
  // app's home composer talks to. It ships with no grants and no toolsets, which
  // makes it useless for this pod; everything below is what lets it actually act.
  "name": "pod_default",
  "description": "The pod's assistant: answers questions about the queue, and can go and change code when asked.",
  "instruction": { "$file": "instruction.md" },
  "toolsets": ["WORKSPACE_CLI", "WEB_SEARCH"],
  "visibility": "POD",
  "permissions": { "grants": [
    { "resource_type": "datastore_table", "resource_name": "signal",
      "permission_ids": ["datastore.table.read", "datastore.record.read", "datastore.record.write"] },
    { "resource_type": "datastore_table", "resource_name": "triage",
      "permission_ids": ["datastore.table.read", "datastore.record.read", "datastore.record.write"] },
    { "resource_type": "datastore_table", "resource_name": "pull_request",
      "permission_ids": ["datastore.table.read", "datastore.record.read", "datastore.record.write"] },
    { "resource_type": "connector", "resource_name": "github", "permission_ids": ["connector.use"] },
    { "resource_type": "connector", "resource_name": "posthog", "permission_ids": ["connector.use"] },
    { "resource_type": "connector", "resource_name": "sentry", "permission_ids": ["connector.use"] },
    { "resource_type": "connector", "resource_name": "linear", "permission_ids": ["connector.use"] },
    { "resource_type": "connector", "resource_name": "googlebigquery", "permission_ids": ["connector.use"] },
    { "resource_type": "agent", "resource_name": "triager", "permission_ids": ["agent.execute"] },
    { "resource_type": "agent", "resource_name": "fixer", "permission_ids": ["agent.execute"] }
  ] }
}
